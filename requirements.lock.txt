# NovaAI Stack — 依赖锁版清单
#
# 生成日期：2026-09-24
# 作者：晨星
#
# ┌─────────────────────────────────────────────────────────────┐
# │ 核心运行依赖：零第三方依赖（仅 Python 3.10+ 标准库）          │
# │ 这是最强的"可复现"保证：git clone 后无需任何 pip install。   │
# └─────────────────────────────────────────────────────────────┘
#
# 第三方依赖计数：0
#
# ── 生产可选依赖（参考锁版，本交付环境未安装以验证，按需启用）──
# fastembed          0.3.5     # Qdrant 开源嵌入（bge-small-zh 等）
# faiss-cpu          1.8.0     # Meta 开源向量索引
# flagembedding      1.3.0     # BGE 跨编码器重排
# pypdf              4.3.1     # PDF 文档加载
#
# 说明：上述版本为参考值；实际安装时以 PyPI 当前可用 wheel 为准。
# 本系统核心路径不依赖以上任何包，verify.py 在裸 CPython 上即全绿。
